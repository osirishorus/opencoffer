# Postgres
# Docker Compose uses postgres://opencoffer:opencoffer@postgres:5432/opencoffer.
# For local non-Docker development use postgres://opencoffer:opencoffer@localhost:5432/opencoffer.
DATABASE_URL=postgres://opencoffer:opencoffer@postgres:5432/opencoffer

# Auth.js
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=replace-me
NEXTAUTH_URL=http://localhost:3000
# Set to 1 to block new registrations after the first user has been created.
DISABLE_REGISTRATION=0

# 32-byte base64 master key for SimpleFIN access URLs + user LLM API keys
# Generate with: openssl rand -base64 32
APP_ENCRYPTION_KEY=replace-me

# Public-facing URL used in MCP setup snippets.
APP_URL=http://localhost:3000

# Worker sync cadence. Defaults to every 30 minutes when unset.
# Cron syntax: minute hour day-of-month month day-of-week
OPENCOFFER_SYNC_CRON=*/30 * * * *

# Scheduled digest summaries (sent only to notification channels that opted
# in via "send scheduled digests"). Default: Mondays at 08:00.
OPENCOFFER_DIGEST_CRON=0 8 * * 1

# Real asset market valuation. All are optional; manual asset values work without them.
# RentCast powers home/land AVM estimates. Auto.dev powers vehicle comparable estimates.
# MarketCheck is optional direct vehicle valuation and may incur per-call data fees.
OPENCOFFER_ASSET_REFRESH_CRON=0 3 * * 0
RENTCAST_API_KEY=
AUTO_DEV_API_KEY=
MARKETCHECK_API_KEY=

# Optional: pre-seeded Ollama base URL inside the compose network
OLLAMA_BASE_URL=http://ollama:11434/v1
